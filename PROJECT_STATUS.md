# 项目状态与下一步

更新时间：2026-08-31 22:13（Asia/Shanghai）

项目汇报版：[TG社群运营工具_项目进度记录_2026-08-31.docx](TG社群运营工具_项目进度记录_2026-08-31.docx)

## 结论摘要

- TG 社群运营工具的查询、关注、管理员新闻推送和召回能力已完成开发；33,491 条交易商数据与核心查询用例已在本机回归通过。
- Bot 进程自 2026-08-28 16:10:47 起持续运行至本次检查，已超过 78 小时；2026-08-31 22:03 自动召回日志记录为 `sent=1 skipped=0 failed=0`。
- 当前仍是 Windows 本地进程，不是正式服务器部署。守护程序持续报告 `Token TXT not found`，因此只能确认“当前进程仍在运行”，不能确认“进程退出后能够自动拉起”。
- 实时新闻/风险自动触发尚未接入外部数据源；Telegram → WikiFX Web/App 的点击、详情访问、安装和深链归因尚未打通。

## 当前已完成

### 1. 自然北极星运营资产

- 已形成自然北极星私域运营闭环的 Markdown、HTML、普通图和高清图。
- 已形成用户归因数据字段图的 HTML、原始图和高清图。
- 已保留三区 2026 Q2 自然北极星数据分析模板，供后续分析与口径核对。

### 2. WikiFX C 端交易商查询与召回方案

- 已形成《WikiFX_C端交易商查询与召回运营方案_20260817.docx》。
- 已形成闭环流程图 PNG，并保留可复现的 Python 生成脚本。
- 已保留相关白板、流程 QA 图和参考图片资产。

### 3. Telegram India 查询 MVP

- 已接入根目录 `交易商名单.xlsx` 中的 33,491 条全量交易商记录，支持名称、公司全称、官网、牌照号和 10 位 WikiFX ID 查询；5 条运营核验数据继续作为官网、牌照号和别名增强层。
- C 端菜单固定为 `/start` 和 `/check`；管理员能力继续按 Telegram User ID 校验，不占用公开菜单。
- 已增加用户主动关注、取消关注、我的关注、管理员手动新闻推送、3 天未查询用户召回和 7 天召回间隔。
- 已验证 `XM` 返回 3 个候选且 `0001461138` 排首位；`xm.com` 和 `0001461138` 均唯一命中 `0001461138`；未知测试词返回无结果。
- 已增加本地一键启动与守护脚本，以及欢迎、查询、新闻和召回演示资产。
- 已完成交易商工作簿生成脚本、MVP 工作簿和操作说明。
- 当前本地事件库共有 68 条聚合事件，其中 `supergroup` 39 条、`private` 29 条；累计记录 16 次查询、14 次唯一匹配、6 次多结果、1 次无结果、14 次结果发送。
- 当前仅有 1 名内部测试用户，活跃关注记录 2 条；这些数据只能证明技术链路，不能作为真实用户转化率或业务成效。

### 4. 运行与稳定性

- 2026-08-31 22:13 检查到一个 Bot 进程树和一个守护进程；Bot 进程创建时间为 2026-08-28 16:10:47，持续运行超过 78 小时。
- 2026-08-28 18:39 日志记录过一次 Telegram 网络连接异常，进程未退出；2026-08-31 22:03 仍成功执行自动召回。
- 守护程序当前每 5 秒记录一次 `Token TXT not found`。现有 Bot 依赖启动时进程环境中的 Token，若进程退出，守护程序无法从安全凭据源重启。
- 结论：连续运行时长已超过 24 小时，但“故障后自动恢复”和“正式持久化部署”尚未验收。

### 5. 版本管理与安全

- 项目位于私有 GitHub 仓库 `Carmybaqi/north-star-operations`；当前版本以 `main` 最新提交为准，发布后核对本地与 `origin/main` 的完整 SHA。
- 根目录已排除 `.env`、Token、密钥、虚拟环境、依赖、本地数据库、事件 CSV、日志、缓存和真实凭据 TXT。
- 项目进度记录纳入版本管理；每次发布前均需重新检查实际差异、文档结构和敏感内容。

## 尚未完成与边界

- 未完成可持续服务器部署和安全 Secret 注入，当前不应表述为“已正式上线”。
- 未完成进程退出后的自动拉起演练，当前不应表述为“已具备故障自愈”。
- 新闻推送仍为管理员手动触发；实时新闻和风险变化自动触发需要外部数据源。
- Telegram 普通 URL 按钮不会回传点击事件，尚不能确认用户是否进入 WikiFX Web、打开 App 下载页、完成安装或进入指定交易商详情页。
- 当前样本仅 1 名内部测试用户，不能计算有效的查询转化率、回访率或业务增量。

## 下一步行动

| 优先级 | 聚焦点 | 具体动作 | 验收标准 | Owner |
| --- | --- | --- | --- | --- |
| P0 | 服务器与凭据 | 提供可持续运行环境；使用平台 Secret 注入 Token、管理员 ID、工作簿和数据库路径；配置进程管理和健康检查 | 主动结束 Bot 后可自动恢复；Token 不进入日志与 Git；连续运行 24 小时无中断 | 技术 |
| P0 | Web/App 追参 | 提供 WikiFX 跟踪跳转服务，记录 `click_id/user_id/broker_id`；补充 GA4 `web_deep_view`、AppsFlyer OneLink 参数和 App `app_deep_view` | 从 Telegram 查询到 Web/App 详情访问可按用户、交易商和来源串联；测试事件可复核 | Web + 数据 + App |
| P1 | 数据质量 | 为交易商工作簿补充更新日期、来源、审核人和回归清单 | 名称、域名、牌照号、别名、ID 和未知词用例全部通过；数据可追溯 | 内容运营 + 合规 |
| P1 | 外部新闻/风险 | 明确可用数据源、触发频率、合规边界和消息审核流程 | 至少 1 个受控数据源完成测试；自动消息可追溯并可人工停用 | 产品 + 技术 + 合规 |
| P1 | 小范围试运行 | 在正式部署和追参完成后，开展 7 天内部/小范围真人测试 | 输出查询人数、有效查询率、未知词率、回访率、WikiFX 跳转率和失败查询复盘 | 运营 + 数据 |

## 2026-09-01 汇报需协调的资源

1. 确认 TG 工具服务器资源和技术 Owner，优先解决安全 Secret 与自动恢复。
2. 指定 Web、GA4、AppsFlyer 和 App 事件负责人，打通 Telegram → WikiFX 详情页的追参和归因。
3. 明确实时新闻/风险数据源及合规审核责任；在数据源确认前继续保持管理员手动推送。
4. 批准正式部署完成后的 7 天小范围试运行，并确定测试群与测试用户范围。

## 当前风险与边界

- GitHub 代码备份不等于 Bot 已部署，也不等于业务闭环已验证。
- 本地进程运行超过 78 小时不等于服务器级可用性；必须补做安全重启和故障恢复验收。
- 聚合事件用于技术验证，仓库不会保存真实 Telegram Token、本地事件数据库或用户明细。
- 未接入 Web/App 追参前，只能报告 Telegram 内部查询、匹配、关注和召回事件，不能报告深访、安装或转化。
